# Runtime dependencies for the web app only (Vercel installs from this file).
# apscheduler and alembic are deliberately absent: the scheduler cannot run on
# serverless, and migrations are applied from a machine with a real shell.
fastapi>=0.115
sqlalchemy>=2.0
psycopg[binary]>=3.2
httpx>=0.28
pydantic>=2.9
python-dotenv>=1.0
selectolax>=0.3.21
pyyaml>=6.0
# Page templates (templates/). Autoescaped, so scraped titles cannot inject markup.
jinja2>=3.1

# Reading bid documents (app/bidpdf.py). Imported lazily, so a deploy without it
# still serves; only enrichment stops.
pypdf>=5.0.0
# Text extraction for the same documents: C, ~20x faster than pypdf's.
pypdfium2>=4.30
# Browser-driven connectors (app/connectors/gem.py). NOT installed on Vercel --
# there is no Chromium in a serverless function, which is why /cron/ingest skips
# connectors that declare requires_browser. Needed on whatever host runs
# run_prod_worker.py.
playwright>=1.47.0

